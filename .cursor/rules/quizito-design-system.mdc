---
description: 
globs: 
alwaysApply: true
---
# 🎨 Quizito Design System - Cursor Rules

## 🌟 Visual Identity & Brand Language

**Quizito** uses a **Dark, Minimal, Vibrant** design language that emphasizes:
- **Intelligence & Innovation** through purple/indigo gradients
- **Energy & Engagement** through vibrant accent colors
- **Premium Quality** through sophisticated dark surfaces
- **Accessibility** through high contrast and clear typography

## 🎨 Color System

### Core Palette
```tsx
// Primary Brand Colors - Use for main CTAs, active states, brand elements
const BRAND_COLORS = {
  primary: '#6366F1',      // Indigo - Main brand color
  primaryHover: '#5B5CF6', // Darker indigo for hover states
  secondary: '#14B8A6',    // Teal - Secondary accent
  secondaryHover: '#10B981' // Darker teal for hover states
};

// Semantic Colors
const SEMANTIC_COLORS = {
  success: '#22C55E',      // Green - Success, correct answers
  error: '#EF4444',        // Red - Errors, wrong answers  
  warning: '#F59E0B',      // Amber - Warnings, pending states
  info: '#3B82F6'          // Blue - Information, neutral actions
};

// Surface & Background Colors
const SURFACES = {
  background: '#0D0D0D',   // Deep black - Page background
  surface: '#1E1E1E',      // Dark gray - Cards, containers
  surfaceHover: '#2A2A2A', // Lighter dark - Hover states, borders
  surfaceActive: '#3A3A3A' // Even lighter - Active states, inputs
};

// Typography Colors
const TEXT_COLORS = {
  primary: '#E0E0E0',      // Light gray - Main text
  secondary: '#A0A0A0',    // Gray - Descriptions, labels
  muted: '#666666',        // Dark gray - Helper text, captions
  white: '#FFFFFF'         // Pure white - Emphasis text
};
```

### Gradient Patterns
```tsx
// Brand Gradients - Use for buttons, headers, emphasis
'bg-gradient-to-r from-[#6366F1] to-[#14B8A6]'      // Primary gradient
'bg-gradient-to-r from-[#5B5CF6] to-[#10B981]'      // Hover gradient
'bg-gradient-to-br from-purple-400 to-sky-400'       // Text gradients
'bg-gradient-to-r from-purple-900/10 via-transparent to-pink-900/10' // Background gradients
```

## 🧱 Typography System

### Hierarchy & Usage
```tsx
// Page Titles - Main page headers (32px-48px)
<PageTitle className="text-4xl md:text-5xl lg:text-6xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-purple-400 to-sky-400">
  Welcome to Quizito
</PageTitle>

// Section Titles - Major sections (24px-32px)  
<SectionTitle className="text-2xl md:text-3xl font-semibold text-[#E0E0E0] mb-4">
  My Quizzes
</SectionTitle>

// Card Titles - Component headers (20px)
<CardTitle className="text-xl font-semibold text-[#E0E0E0] mb-2">
  Quiz Settings
</CardTitle>

// Body Text - Main content (16px)
<BodyText className="text-base text-[#A0A0A0] leading-relaxed">
  Transform knowledge into engaging quizzes
</BodyText>

// Muted Text - Helper text (14px)
<MutedText className="text-sm text-[#666666]">
  Optional description
</MutedText>
```

### Font Properties
- **Primary Font**: Inter, system-ui, sans-serif
- **Font Weight**: 400 (normal), 500 (medium), 600 (semibold), 700 (bold)
- **Line Height**: 1.5 for body text, 1.2 for headings
- **Letter Spacing**: -0.025em for large text, normal for body

## 🔧 Component Standards

### Buttons
```tsx
// Primary Button - Main CTAs
<Button className="bg-gradient-to-r from-[#6366F1] to-[#14B8A6] hover:from-[#5B5CF6] hover:to-[#10B981] text-white font-medium transition-all duration-300 shadow-lg hover:shadow-xl hover:scale-105">

// Secondary Button - Secondary actions
<Button className="bg-[#1E1E1E] border border-[#2A2A2A] hover:border-[#6366F1]/50 hover:bg-[#2A2A2A] text-[#E0E0E0] transition-all duration-200">

// Ghost Button - Subtle actions  
<Button variant="ghost" className="text-[#A0A0A0] hover:text-white hover:bg-[#2A2A2A] transition-all duration-200">

// Destructive Button - Delete/dangerous actions
<Button variant="destructive" className="bg-[#EF4444] hover:bg-[#DC2626] text-white transition-all duration-200">
```

### Cards & Surfaces
```tsx
// Standard Card
<Card className="bg-[#1E1E1E]/90 backdrop-blur-sm border-[#2A2A2A] shadow-xl shadow-purple-500/10 hover:border-purple-500/30 transition-all duration-300">

// Interactive Card - Hover effects
<Card className="bg-[#1E1E1E]/90 backdrop-blur-sm border-[#2A2A2A] shadow-xl shadow-purple-500/10 hover:border-[#6366F1]/50 hover:shadow-2xl hover:shadow-purple-500/20 transition-all duration-300 cursor-pointer">

// Elevated Card - Important content
<Card className="bg-[#1E1E1E]/95 backdrop-blur-md border-[#2A2A2A] shadow-2xl shadow-purple-500/15">
```

### Form Elements
```tsx
// Input Fields
<Input className="bg-[#2A2A2A]/50 border-[#3A3A3A] text-[#E0E0E0] placeholder:text-[#666666] focus:border-[#6366F1]/50 focus:ring-[#6366F1]/20 transition-all duration-200">

// Textarea
<Textarea className="bg-[#2A2A2A]/50 border-[#3A3A3A] text-[#E0E0E0] placeholder:text-[#666666] focus:border-[#6366F1]/50 focus:ring-[#6366F1]/20 transition-all duration-200">

// Labels
<Label className="text-sm font-medium text-[#E0E0E0]">

// Form sections
<div className="space-y-4 p-6 rounded-lg bg-[#1E1E1E]/50 border border-[#2A2A2A]">
```

### Navigation & Layout
```tsx
// Navbar styling
<nav className="bg-[#0D0D0D]/95 backdrop-blur-lg border-b border-[#2A2A2A]">

// Sidebar elements
<div className="bg-[#1E1E1E] border-r border-[#2A2A2A]">

// Footer
<footer className="bg-[#0D0D0D] border-t border-[#2A2A2A]">
```

## 🎭 Animation & Interactions

### Standard Animation Variants
```tsx
// Container animations - For page/section entry
const containerVariants = {
  hidden: { opacity: 0 },
  visible: {
    opacity: 1,
    transition: { 
      staggerChildren: 0.1,
      delayChildren: 0.1
    }
  }
};

// Item animations - For individual elements
const itemVariants = {
  hidden: { y: 20, opacity: 0 },
  visible: { 
    y: 0, 
    opacity: 1,
    transition: { duration: 0.5, ease: "easeOut" }
  }
};

// Button hover animations
const buttonHover = {
  scale: 1.05,
  transition: { duration: 0.2, ease: "easeInOut" }
};
```

### Hover Effects
- **Buttons**: Scale 1.05, shadow enhancement, gradient shifts
- **Cards**: Border color change, shadow intensification, slight scale (1.02)
- **Icons**: Scale 1.1, color change to brand colors
- **Links**: Color transition, underline effects

### Loading States
```tsx
// Skeleton loading
<div className="h-4 bg-[#2A2A2A] animate-pulse rounded-md" />

// Button loading
<Button disabled className="bg-[#2A2A2A] text-[#666666]">
  <Loader2 className="mr-2 h-4 w-4 animate-spin" />
  Loading...
</Button>
```

## 🌟 Background Effects

### Page Background System
```tsx
// Hero pages (landing)
<PageBackground variant="hero">
  {/* 50 particles, full gradient orbs */}
</PageBackground>

// App pages (dashboard, forms)
<PageBackground variant="default">
  {/* 25 particles, moderate effects */}
</PageBackground>

// Minimal pages (auth, loading)
<PageBackground variant="minimal">
  {/* 15 particles, subtle effects */}
</PageBackground>
```

### Gradient Overlays
- **Base**: `from-purple-900/10 via-transparent to-pink-900/10`
- **Intense**: `from-purple-900/20 via-blue-900/10 to-teal-900/20`
- **Subtle**: `from-purple-900/5 via-transparent to-pink-900/5`

## 📱 Responsive Design

### Breakpoints
- **Mobile**: < 768px (stack layouts, larger touch targets)
- **Tablet**: 768px - 1024px (hybrid layouts, medium spacing)
- **Desktop**: > 1024px (full layouts, optimal spacing)

### Mobile-First Patterns
```tsx
// Typography scaling
className="text-lg md:text-xl lg:text-2xl"

// Padding/margin scaling  
className="p-4 md:p-6 lg:p-8"

// Grid responsive
className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 md:gap-6"
```

## ✅ Implementation Rules

### ALWAYS Use:
1. **AppLayout** wrapper for all pages with appropriate `backgroundVariant`
2. **Typography components** instead of raw text elements
3. **Consistent spacing** using Tailwind's spacing scale
4. **Semantic color variables** instead of hardcoded hex values
5. **Motion components** for page transitions and interactions
6. **Proper contrast ratios** for accessibility

### NEVER Use:
1. Pure white (#FFFFFF) for backgrounds
2. Pure black (#000000) except for deep shadows
3. Bright, saturated colors outside the defined palette
4. Animations longer than 500ms for UI interactions
5. Hardcoded colors that bypass the design system

### Component Structure Pattern:
```tsx
export default function YourPage() {
  return (
    <AppLayout backgroundVariant="default">
      <motion.div 
        variants={containerVariants} 
        initial="hidden" 
        animate="visible"
        className="container mx-auto px-4 py-8"
      >
        <motion.div variants={itemVariants}>
          <PageTitle>Your Page Title</PageTitle>
          <BodyText>Your page description</BodyText>
        </motion.div>
        
        <motion.div variants={itemVariants} className="mt-8">
          {/* Page content */}
        </motion.div>
      </motion.div>
    </AppLayout>
  );
}
```

This design system ensures **visual consistency**, **brand recognition**, and **exceptional user experience** across the entire Quizito application while maintaining **accessibility** and **performance** standards.
