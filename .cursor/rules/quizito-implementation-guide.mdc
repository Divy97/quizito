---
description: 
globs: 
alwaysApply: true
---
# 🚀 Quizito Design System Implementation Guide

## 📋 Implementation Roadmap

This guide outlines the systematic approach to transform the entire Quizito app with the new glassmorphism design system. We'll update components and pages in the optimal order to ensure consistency.

## 🎯 Implementation Priority Order

### **Phase 1: Foundation (Core System)**
1. **CSS Variables Update** - [globals.css](mdc:frontend/src/app/globals.css)
2. **Base Components** - Button, Card, Input, Typography
3. **Layout Wrapper** - [layout.tsx](mdc:frontend/src/app/layout.tsx)

### **Phase 2: Navigation & Layout**
4. **Navigation Bar** - [navbar.tsx](mdc:frontend/src/components/ui/navbar.tsx)
5. **Footer Component** - [footer.tsx](mdc:frontend/src/components/ui/footer.tsx)
6. **App Layout** - [app-layout.tsx](mdc:frontend/src/components/ui/app-layout.tsx)

### **Phase 3: Core Pages**
7. **Landing Page** - [page.tsx](mdc:frontend/src/app/page.tsx)
8. **Hero Section** - [HeroSection.tsx](mdc:frontend/src/components/landing/HeroSection.tsx)
9. **Landing Components** - All landing sections

### **Phase 4: App Pages**
10. **Quiz Creation** - [create/page.tsx](mdc:frontend/src/app/create/page.tsx)
11. **My Quizzes** - [my-quizzes/page.tsx](mdc:frontend/src/app/my-quizzes/page.tsx)  
12. **Quiz Player** - [QuizPlayer.tsx](mdc:frontend/src/components/quiz/QuizPlayer.tsx)
13. **Quiz Results** - Quiz results components

## 🔧 Component Transformation Patterns

### **CSS Variables Replacement Map**
```css
/* OLD → NEW */
#0D0D0D → var(--quizito-bg-primary)
#1E1E1E → var(--quizito-glass-surface)
#2A2A2A → var(--quizito-glass-border)
#6366F1 → var(--quizito-electric-blue)
#14B8A6 → var(--quizito-neon-purple)
#E0E0E0 → var(--quizito-text-primary)
#A0A0A0 → var(--quizito-text-secondary)
#666666 → var(--quizito-text-muted)
```

### **Button Transformation**
```tsx
// OLD BUTTON
<Button className="bg-gradient-to-r from-[#6366F1] to-[#14B8A6] hover:from-[#5B5CF6] hover:to-[#10B981] text-white transition-all duration-300">

// NEW ELECTRIC BUTTON
<Button className="
  bg-gradient-to-r from-[var(--quizito-electric-blue)] to-[var(--quizito-neon-purple)]
  text-white font-semibold
  px-8 py-4 rounded-2xl
  shadow-[0_0_20px_rgba(0,212,255,0.4)]
  hover:shadow-[0_0_30px_rgba(0,212,255,0.6)]
  hover:scale-105
  transition-all duration-300
  border-0
">
```

### **Card Transformation**
```tsx
// OLD CARD
<Card className="bg-[#1E1E1E]/90 backdrop-blur-sm border-[#2A2A2A] shadow-xl shadow-purple-500/10">

// NEW GLASS CARD
<Card className="
  bg-[var(--quizito-glass-surface)] 
  backdrop-blur-xl 
  border border-[var(--quizito-glass-border)] 
  rounded-2xl 
  p-6 
  shadow-2xl shadow-black/20
  hover:bg-[var(--quizito-glass-surface-hover)] 
  hover:border-[var(--quizito-glass-border-hover)]
  transition-all duration-300
">
```

### **Background Transformation**
```tsx
// OLD BACKGROUND
<div className="min-h-screen bg-[#0D0D0D] text-[#E0E0E0]">

// NEW GLASS BACKGROUND
<div className="min-h-screen bg-[var(--quizito-bg-primary)]">
  {/* Background gradient overlay */}
  <div className="fixed inset-0 bg-gradient-to-br from-[var(--quizito-bg-primary)] via-[var(--quizito-bg-secondary)] to-[var(--quizito-bg-primary)] -z-10" />
  
  {/* Content */}
  <div className="relative z-10">
    {children}
  </div>
</div>
```

## 📁 File-by-File Transformation Checklist

### **1. globals.css**
- [ ] Replace all color variables with new electric palette
- [ ] Add glassmorphism utility classes
- [ ] Add glow effect utilities
- [ ] Update dark theme variables

### **2. layout.tsx**
- [ ] Add `className="dark"` to html element
- [ ] Update body background to use CSS variables
- [ ] Ensure backdrop-blur support

### **3. Navbar Component**
- [ ] Glass navbar background with blur
- [ ] Electric blue logo gradient
- [ ] Hover effects on navigation items
- [ ] Electric primary button for CTA

### **4. Landing Page Components**
- [ ] **HeroSection**: Electric gradient hero title, glass buttons
- [ ] **StepsSection**: Glass cards with electric accents
- [ ] **InputTypesSection**: Neon glow on hover for input types
- [ ] **CTASection**: Electric gradient call-to-action

### **5. App Layout Wrapper**
- [ ] Glass background system
- [ ] Floating particles/orbs effect
- [ ] Smooth page transitions

### **6. Form Components**
- [ ] Glass input fields with electric focus states
- [ ] Glass textareas and selects
- [ ] Electric blue focus rings
- [ ] Smooth transitions

### **7. Quiz Components**
- [ ] **QuizPlayer**: Glass quiz cards, electric progress bars
- [ ] **Quiz Options**: Glass buttons with hover glow
- [ ] **Results**: Electric success/error states
- [ ] **Leaderboard**: Glass table with electric accents

## 🎨 Quick Reference: Class Patterns

### **Glass Card Pattern**
```tsx
"bg-[var(--quizito-glass-surface)] backdrop-blur-xl border border-[var(--quizito-glass-border)] rounded-2xl p-6 shadow-2xl shadow-black/20 hover:bg-[var(--quizito-glass-surface-hover)] hover:border-[var(--quizito-glass-border-hover)] transition-all duration-300"
```

### **Electric Button Pattern**
```tsx
"bg-gradient-to-r from-[var(--quizito-electric-blue)] to-[var(--quizito-neon-purple)] text-white font-semibold px-8 py-4 rounded-2xl shadow-[0_0_20px_rgba(0,212,255,0.4)] hover:shadow-[0_0_30px_rgba(0,212,255,0.6)] hover:scale-105 transition-all duration-300"
```

### **Glass Input Pattern**
```tsx
"bg-[var(--quizito-glass-surface)] backdrop-blur-xl border border-[var(--quizito-glass-border)] text-[var(--quizito-text-primary)] px-4 py-3 rounded-xl focus:border-[var(--quizito-electric-blue)] focus:shadow-[0_0_0_3px_rgba(0,212,255,0.1)] transition-all duration-300"
```

### **Hero Title Pattern**
```tsx
"text-5xl md:text-6xl font-bold bg-gradient-to-r from-[var(--quizito-electric-blue)] via-[var(--quizito-neon-purple)] to-[var(--quizito-cyber-green)] bg-clip-text text-transparent"
```

### **Page Background Pattern**
```tsx
"min-h-screen bg-[var(--quizito-bg-primary)] relative"
// + gradient overlay div
// + content wrapper with z-10
```

## 🔍 Quality Assurance Checklist

For each component/page updated, verify:
- [ ] Uses CSS variables instead of hardcoded colors
- [ ] Has appropriate glassmorphism effects
- [ ] Electric blue used for primary actions
- [ ] Smooth animations (≤300ms)
- [ ] Proper hover states with glow effects
- [ ] Rounded corners (12px-24px)
- [ ] Consistent spacing (4px multiples)
- [ ] Mobile responsive design
- [ ] Accessible contrast ratios
- [ ] No layout shifts during animations

## 🚨 Common Pitfalls to Avoid

1. **Don't mix old and new colors** in the same component
2. **Don't use harsh shadows** - use glow effects instead
3. **Don't animate expensive properties** - stick to transform/opacity
4. **Don't skip backdrop-blur** on glass elements
5. **Don't use sharp corners** - always round (min 8px)
6. **Don't exceed 3 accent colors** in one view
7. **Don't make animations too long** - max 500ms
8. **Don't forget hover states** - every interactive element needs one

## 📱 Mobile Considerations

- Use `backdrop-blur-sm` instead of `backdrop-blur-xl` on mobile for performance
- Reduce glow effects intensity on smaller screens
- Ensure touch targets are minimum 44px
- Test glassmorphism effects on various mobile browsers
- Consider reduced motion preferences

## 🎯 Success Metrics

The transformation is successful when:
- **Visual Consistency**: All pages use the same color palette and effects
- **Brand Recognition**: UI feels distinctly "Quizito" 
- **Performance**: No janky animations or layout shifts
- **Accessibility**: Maintains WCAG contrast standards
- **Mobile Experience**: Works smoothly on all devices
- **User Feedback**: Users notice and appreciate the modern feel

This systematic approach ensures we transform Quizito into a modern, glassmorphism-powered app that users will love and remember!
